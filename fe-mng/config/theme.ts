import { type ThemeConfig } from 'antd';

export const systemTheme: ThemeConfig = {
  // algorithm: theme.darkAlgorithm,
  cssVar: true,
  hashed: false,
  token: {
    colorPrimary: '#2563eb',
    borderRadius: 8,
    wireframe: false,
  },
};
